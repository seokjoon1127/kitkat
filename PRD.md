# kitkat PRD — 에이전트용 로컬 영상 편집 엔진 (작업 중 문서)

버전: v0.1 (2026-09-01) · 상태: 결정 진행 중

## 1. 목적

쇼츠·광고를 만드는 에이전트에 "영상 편집 도구"로 붙는 로컬 프로그램.
CapCut의 기능을 로컬에서 재현하되, 판매용 제품이 아니라 에이전트의 손발이다.

- 에이전트가 API로 편집을 수행한다.
- 사람은 에이전트가 준 URL을 브라우저로 열어 결과를 보고, 필요하면 직접 타임라인에서 편집한다 (§3 D3).
- 결과물 품질은 CapCut 동급 이상. 타협 없음.

## 2. 디자인 필라

모든 이후 결정은 이 5개에 비추어 판단한다.

- **P1 에이전트 우선** — 모든 기능은 사람 손보다 API로 먼저 열린다. 사람 UI는 같은 문서를 보는 또 하나의 창일 뿐이다.
- **P2 하나의 문서** — 프로젝트는 JSON 타임라인 문서 하나가 진실의 원천. 에이전트·편집 UI·렌더러 셋 다 이 문서만 읽고 쓴다.
- **P3 결과물 무타협, 미리보기는 프록시** — 최종 렌더 품질은 CapCut 동급 이상. 편집 중 미리보기는 저해상도 사본(프록시)으로 부드러움을 확보한다.
- **P4 전부 로컬** — 클라우드 서비스 없이 내 컴퓨터에서 다 돈다.
- **P5 개수보다 시스템** — 전환 500개가 목표가 아니라, 전환·효과·자막 스타일을 쉽게 추가할 수 있는 구조가 목표. 개수는 나중에 채운다.

## 3. 확정된 결정 (로그)

- **D1** (2026-09-01) 앱 형태: 데스크톱 앱이 아니라 **로컬 웹 서버** — 에이전트용 API 문 + 사람용 URL 문.
- **D2** (2026-09-01) "성능"의 정의: 승부처는 결과물 품질·렌더 속도이며 여기서 CapCut 동급 이상. 편집 화면 반응성은 프록시 미리보기로 해결한다 (P3).
- **D3** (2026-09-01) URL의 역할: 미리보기 전용이 아니라 **풀 타임라인 편집기**. 동시에 에이전트도 같은 프로젝트를 직접 편집할 수 있어야 한다 → 사람과 에이전트가 같은 문서를 공유·동기화 (P2).
- **D4** (2026-09-01) 편집 UI는 **자체 제작** (OpenCut은 참고서로만). 근거: "모든 기능을 사람이 직접" + "뛰어난 성능" 요구는 남의 코드 구조 위에서 달성하기 어렵고, 미리보기 엔진(WebCodecs 하드웨어 디코딩 + WebGL 합성)을 직접 쥐어야 성능을 끝까지 끌어올릴 수 있음.
- **D5** (2026-09-01) **기능 패리티**: 모든 편집 기능은 에이전트 API와 사람 편집 UI 양쪽에서 똑같이 가능해야 한다. 한쪽에만 있는 기능은 만들지 않는다 (P1·P2의 따름정리).
- **D6** (2026-09-01, 기술 기본값) 에이전트 연결: **HTTP API가 본체, MCP 서버는 그 위의 얇은 래퍼** — 둘 다 제공. 이견 있으면 변경 가능.
- **D7** (2026-09-01, 기술 기본값) 서버 스택: **Node/TypeScript 단일 서버** (Remotion·편집 UI와 같은 생태계) + AI 모델(Whisper 등)은 **Python 보조 프로세스**로 분리. 이견 있으면 변경 가능.

## 4. 시스템 구조

```
[에이전트] ──(MCP/HTTP API)──┐
                             ├──> 프로젝트 문서 (JSON 타임라인) ──> [렌더러: FFmpeg + Remotion] ──> mp4 / 미리보기 URL
[사람: 브라우저 편집 UI] ────┘        ↑ WebSocket 실시간 동기화
```

- 에이전트가 문서를 고치면 열려 있는 편집 화면에 즉시 반영되고, 사람이 고친 것도 에이전트가 다음 호출 때 그대로 본다.
- 렌더러: **FFmpeg**(자르기·배속·인코딩·GPU 압축 = 속도 담당) + **Remotion**(자막·글자 애니메이션·효과·모션그래픽 = 때깔 담당).
- AI 기능은 같은 서버의 내부 기능으로 추가 (§5 ③군).

## 5. 기능 범위 (CapCut 대비)

### ①군 — 파일 처리 (전부 구현)
자르기/분할/트림 · 이어붙이기/순서/복제/교체 · 배속(0.1~100배) · 역재생 · 정지화면 · 사진→클립 · 회전/반전/크롭 · 화면비 변경+여백 채우기(블러/단색/이미지) · 소리 추출/음소거 · 볼륨/페이드 · 손떨림 보정 · 내보내기(해상도·fps·MP4/MOV/GIF·구간·알파) · 커버 지정

### ②군 — 실시간 합성 (전부 구현)
멀티트랙 타임라인 · 키프레임 애니메이션 · 오버레이(PiP)+블렌딩 모드 · 마스크+페더 · 전환 효과 · 필터+LUT · 세부 색조정(밝기~커브) · 화면 효과 · 크로마키 · 커브 속도 · 텍스트(스타일+애니메이션+템플릿) · 스티커/GIF · 보이스오버 녹음 · 트랙 믹싱 · 비트 마커 · 피치 보정 · 더킹

### ③군 — 로컬 AI (모델 확정, 순차 구현)
자동자막(Whisper) · TTS(Kokoro/Piper) · 인물 배경 제거(RVM/BiRefNet) · 보컬 분리(Demucs) · 잡음 제거(RNNoise/DeepFilterNet) · 업스케일(Real-ESRGAN) · 프레임 보간(RIFE) · 음성 변조(RVC) · 이미지 생성(SD 계열, 사양 허용 시)

### ④군 — 하지 않는 것
템플릿 마켓 · 상용 음악/폰트/스티커 라이브러리(무료 자산으로 대체) · 클라우드 동기화/협업 · SNS 업로드 연동

## 6. 빌드 순서

- **0단계 프로젝트 문서 스키마** — JSON 타임라인 명세 설계. 모든 것의 심장 (P2).
- **1단계 렌더 파이프라인** — 문서 → mp4 (FFmpeg+Remotion).
- **2단계 에이전트 API/MCP** — 문서를 만들고 수정하는 도구들. ★여기까지만 되면 에이전트로 쇼츠 생산 시작 가능.
- **3단계 미리보기 URL** — 렌더 결과를 브라우저로 보는 페이지 + 실시간 동기화 기반.
- **4단계 타임라인 편집 UI** — 풀 편집기 (D3). 개발량 최대 구간.
- **5단계 ③군 AI 기능** — Whisper 자동자막부터 하나씩. (2단계 이후 병행 가능)

## 7. 미해결 갈림길

- **[TBD-3]** 프로젝트 문서 스키마 상세 — 0단계에서 설계한다.
- (TBD-1 → D4로, TBD-2 → D6으로, TBD-4 → D7로 확정됨)
